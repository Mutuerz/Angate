using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This Script allows to keep track of the events that the player has executed
public class GlobalEvents : MonoBehaviour {
   public bool PowerOn = false;// indicates that the player has power on the energy backup
   public bool SecurityDoorOpened = false;// indicates if the security door was opend at leats one time
	
}
